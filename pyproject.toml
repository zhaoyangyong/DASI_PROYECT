[project]
name = "fdi-dasi-jackson"
version = "0.2.0"
description = "Resource-exchange agent for a multi-agent trading game"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "loguru",
]
